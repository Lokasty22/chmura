apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: brilliant
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: zad2-frontend:1.0
          ports:
            - containerPort: 80
